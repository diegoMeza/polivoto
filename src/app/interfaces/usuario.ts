/**
 * Created by darkklitos on 15/04/17.
 */
export class Usuario {
  uid? : string;
  nombre? : string;
  email ? : string;
  cedula ? : number;
  genero ? : string;
  password ? : string;
  idEmpresa ? : string;
  fechaNacimiento ? : Date;
}
