import { ValidaVotacionPipe } from './valida-votacion.pipe';

describe('ValidaVotacionPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidaVotacionPipe();
    expect(pipe).toBeTruthy();
  });
});
