import { ValidaSiVotoPipe } from './valida-si-voto.pipe';

describe('ValidaSiVotoPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidaSiVotoPipe();
    expect(pipe).toBeTruthy();
  });
});
